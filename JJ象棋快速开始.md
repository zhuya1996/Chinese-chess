# JJ 象棋 AI 助手 - 5分钟快速开始

## 🎯 目标

在 JJ 象棋 App 中实时获得 AI 分析建议

---

## ⚡ 快速步骤

### 1️⃣ 检查 ADB（2分钟）

**双击运行**：[检查ADB环境.bat](检查ADB环境.bat)

**如果提示 ADB 未安装**：
- 下载：https://developer.android.com/studio/releases/platform-tools
- 解压到 `C:\adb\`
- 添加到系统 PATH：
  1. 右键 **此电脑** → **属性** → **高级系统设置**
  2. **环境变量** → **系统变量** → **Path** → **编辑**
  3. **新建** → 输入 `C:\adb` → **确定**
  4. 重新打开命令提示符测试：`adb version`

---

### 2️⃣ 准备手机（1分钟）

**开启 USB 调试**：
1. **设置** → **关于手机** → 连续点击 **版本号** 7次
2. **设置** → **系统** → **开发者选项** → 开启 **USB 调试**
3. 用 USB 线连接手机和电脑
4. 手机弹出授权对话框 → 勾选 **始终允许** → **确定**

**验证连接**：
```bash
adb devices
# 应该显示你的设备
```

---

### 3️⃣ 启动 Web 服务（30秒）

**双击运行**：[启动服务_修复版.bat](启动服务_修复版.bat)

浏览器自动打开：http://localhost:5000

点击 **"启动引擎"** 按钮，等待 **"引擎就绪"**

---

### 4️⃣ 打开 JJ 象棋（30秒）

1. 在手机上打开 **JJ象棋 App**
2. 进入 **对局界面**（人人对战或人机对战都行）
3. 确保 **棋盘完整显示**
4. 建议使用 **标准皮肤**（默认）

---

### 5️⃣ 启动识别（1分钟）

在 Web 界面：
1. 切换到 **"识别助手"** Tab（右侧第3个）
2. 点击 **"启动 JJ 识别"** 按钮
3. 会显示手机屏幕画面

**首次使用 - 四角校准**（必须！）：
```
依次点击棋盘四个角的交叉点：

1. 左上角 ←┐     (红方左车位置)
2. 右上角  ┌→    (红方右车位置)
3. 左下角 ←┘     (黑方左车位置)
4. 右下角  └→    (黑方右车位置)
```

选择视角（通常选 **红方在下**）

点击 **"确认校准"**

系统会自动识别并生成棋子模板（约10秒）

---

### 6️⃣ 实时使用（持续）

**自动同步**：
- 识别助手会实时抓取屏幕
- 当置信度 > 78% 时自动同步局面
- Web 界面棋盘会自动更新

**获取 AI 建议**：
- 按键盘 **A** 键
- 或点击 **"获取建议"** 按钮
- 查看最佳着法、评估值、主要变化

**查看多个候选**：
- 按键盘 **M** 键
- 或点击 **"多个候选"** 按钮
- 同时看到 5 个候选着法

**手动走棋**：
- 在 JJ 象棋 App 中走 AI 建议的着法

---

## 🎮 完整操作流程图

```
┌─────────────────────┐
│ 1. 连接手机 (USB)   │
│    adb devices       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 2. 启动 Web 服务    │
│    localhost:5000    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 3. 启动引擎         │
│    点击按钮         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 4. 打开 JJ 象棋     │
│    进入对局界面     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 5. 启动识别         │
│    ��角校准         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 6. 自动识别局面     │
│    置信度 > 78%     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 7. 获取 AI 建议 (A) │
│    查看评估         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 8. 在 JJ 中走棋     │
│    重复 6-8         │
└─────────────────────┘
```

---

## 🐛 常见问题速查

### ❌ adb 命令不存在
→ ADB 未安装或未添加到 PATH
→ 下载 platform-tools 并配置环境变量

### ❌ adb devices 无设备
→ USB 未连接或未开启 USB 调试
→ 检查 USB 线、手机设置

### ❌ 设备显示 unauthorized
→ 手机未授权 USB 调试
→ 手机上重新授权并勾选"始终允许"

### ❌ 识别置信度低 (< 50%)
→ 棋盘未完整显示或皮肤不支持
→ 重新校准四角，切换到标准皮肤

### ❌ 识别结果不同步
→ 置信度阈值太高或稳定帧不足
→ 降低置信度到 70%，降低稳定帧到 1

### ❌ Web 界面无响应
→ 服务未启动或引擎未就绪
→ 刷新页面，重新启动引擎

---

## 📊 识别状态指示

| 状态 | 说明 | 建议 |
|------|------|------|
| 置信度 < 50% | 识别失败 | 重新校准 |
| 置信度 50-78% | 识别中等 | 观察或调整阈值 |
| 置信度 > 78% | 识别成功 | 正常使用 ✓ |
| 稳定帧 0-1 | 局面变化中 | 等待稳定 |
| 稳定帧 ≥ 2 | 局面稳定 | 自动同步 ✓ |

---

## ⚠️ 重要提示

### 合规使用
- ✅ **学习研究**：复盘分析、研究开局、练习残局
- ✅ **人机对战**：与电脑练习
- ❌ **竞技赛**：排位赛、锦标赛
- ❌ **真人对战**：可能违反游戏规则

### 账号安全
- 使用自动执行功能可能导致账号封禁
- 建议仅手动查看建议，手动走棋
- **风险自负**

---

## 🛠️ 测试工具

### 环境检查
```bash
双击：检查ADB环境.bat
```

### 识别测试
```bash
python 测试手机识别.py
```

### API 测试
```bash
python test_api.py
```

---

## 📚 详细文档

需要更多信息？查看：
- [JJ象棋使用指南.md](JJ象棋使用指南.md) - 完整详细说明
- [使用指南.md](使用指南.md) - Web 界面操作
- [成功运行指南.md](成功运行指南.md) - 技术细节

---

## ⏱️ 时间估算

- **首次设置**：约 5-10 分钟（含 ADB 安装）
- **后续使用**：约 1 分钟（启动服务 + 识别）
- **校准**：仅首次需要，约 1 分钟

---

## 🎉 开始使用

准备好了吗？

1. **双击**：[检查ADB环境.bat](检查ADB环境.bat)
2. **双击**：[启动服务_修复版.bat](启动服务_修复版.bat)
3. **浏览器**：http://localhost:5000
4. **手机**：打开 JJ 象棋
5. **Web**：识别助手 → 启动 JJ 识别

**祝你棋艺精进！** 🎊
