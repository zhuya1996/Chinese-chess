# 启动服务故障排除指南

## 🔍 第一步：诊断问题

**双击运行**：`诊断环境.bat`

这会自动检查：
- ✅ 引擎文件是否存在
- ✅ Python 是否安装
- ✅ Flask 是否安装
- ✅ 端口是否被占用
- ✅ 网络 IP 地址

---

## 🐛 常见错误和解决方案

### 错误 1：找不到 Python

**症状**：
```
'python' 不是内部或外部命令
```

**原因**：Python 未安装或未添加到 PATH

**解决方案**：

**方法一：使用现有的 Anaconda Python**
```bash
# 直接运行
C:\ProgramData\Anaconda3\python.exe web_assistant.py
```

**方法二：添加到 PATH**
1. 右键"此电脑" → 属性 → 高级系统设置
2. 环境变量 → 系统变量 → Path → 编辑
3. 新建 → 输入 `C:\ProgramData\Anaconda3`
4. 新建 → 输入 `C:\ProgramData\Anaconda3\Scripts`
5. 确定 → 重新打开命令提示符

**方法三：安装 Python**
- 下载：https://www.python.org/downloads/
- 安装时勾选 "Add Python to PATH"

---

### 错误 2：ModuleNotFoundError: No module named 'flask'

**症状**：
```
ModuleNotFoundError: No module named 'flask'
```

**原因**：Flask 未安装

**解决方案**：

```bash
# 使用 Anaconda Python 安装
C:\ProgramData\Anaconda3\python.exe -m pip install Flask==2.3.0 Werkzeug==2.3.0

# 或使用系统 Python
python -m pip install Flask==2.3.0 Werkzeug==2.3.0

# 或双击运行
安装依赖.bat
```

---

### 错误 3：端口 5000 被占用

**症状**：
```
Address already in use
Port 5000 is in use by another program
```

**原因**：端口 5000 被其他程序占用

**解决方案**：

**方法一：停止占用端口的程序**
```bash
# 查找占用端口的程序
netstat -ano | findstr ":5000"

# 停止该程序（替换 PID）
taskkill /F /PID <进程ID>
```

**方法二：使用其他端口**
```bash
# 使用 8080 端口
python web_assistant.py --port 8080

# 然后访问
http://localhost:8080
```

**方法三：停止所有 Python 进程**
```bash
taskkill /F /IM python.exe
```

---

### 错误 4：引擎启动失败

**症状**：
```
引擎启动失败
FileNotFoundError: pikafish.exe
```

**原因**：引擎文件不存在或路径错误

**解决方案**：

1. **检查文件是否存在**
   ```bash
   dir src\pikafish.exe
   dir src\pikafish.nnue
   ```

2. **如果不存在**，参考之前的下载指南：
   - 下载：https://github.com/official-pikafish/Pikafish/releases
   - 解压并复制到 `src/` 目录

3. **如果存在但无法运行**：
   - 可能是 CPU 不支持该版本
   - 尝试下载 `popcnt` 或 `sse41` 版本

---

### 错误 5：编码错误（乱码）

**症状**：
```
UnicodeEncodeError: 'gbk' codec can't encode character
```

**原因**：Windows 命令行编码问题

**解决方案**：

**方法一：在脚本开头添加**
```bash
chcp 65001 >nul
```

**方法二：使用 PowerShell**
```powershell
python web_assistant.py
```

**方法三：修改系统区域设置**
1. 控制面板 → 时钟和区域 → 区域
2. 管理 → 更改系统区域设置
3. 勾选"Beta: 使用 Unicode UTF-8"

---

### 错误 6：权限不足

**症状**：
```
Permission denied
Access is denied
```

**原因**：没有管理员权限

**解决方案**：

右键脚本 → 以管理员身份运行

---

## 📋 快速解决步骤

### 方案 A：最简单（推荐）

```bash
# 1. 双击运行
诊断环境.bat

# 2. 查看诊断结果，修复问题

# 3. 双击运行
启动服务_简单版.bat
```

### 方案 B：命令行启动

```bash
# 1. 打开命令提示符（管理员）
Win + X → 命令提示符（管理员）

# 2. 切换到项目目录
cd /d E:\XQ\Chinese-chess

# 3. 直接运行
C:\ProgramData\Anaconda3\python.exe web_assistant.py

# 或
python web_assistant.py
```

### 方案 C：使用 PowerShell

```powershell
# 1. 打开 PowerShell
Win + X → Windows PowerShell

# 2. 切换目录
cd E:\XQ\Chinese-chess

# 3. 运行
python web_assistant.py
```

---

## 🔧 手动测试各组件

### 测试 1：Python 可用性

```bash
# 测试 Anaconda Python
C:\ProgramData\Anaconda3\python.exe --version

# 测试系统 Python
python --version
```

**期望输出**：`Python 3.x.x`

### 测试 2：Flask 可用性

```bash
# 测试 Flask
python -c "import flask; print(flask.__version__)"
```

**期望输出**：`2.0.3` 或类似版本号

### 测试 3：引擎可用性

```bash
# 进入 src 目录
cd src

# 测试引擎
echo quit | pikafish.exe
```

**期望输出**：引擎信息和版本号

### 测试 4：Web 服务

```bash
# 启动服务
python web_assistant.py

# 另开一个命令提示符测试
curl http://localhost:5000
```

**期望输出**：HTML 页面内容

---

## 📱 移动端测试

### 测试移动端页面

```bash
# 启动服务后，访问
http://localhost:5000/mobile
```

**期望效果**：看到紫色渐变的 AI 助手卡片

### 测试移动端 API

```bash
# 测试状态
curl http://localhost:5000/api/mobile/status

# 测试建议
curl -X POST http://localhost:5000/api/mobile/quick_suggest ^
  -H "Content-Type: application/json" ^
  -d "{\"depth\": 12}"
```

---

## 🆘 如果仍然无法解决

### 收集错误信息

1. **运行诊断脚本**
   ```bash
   诊断环境.bat > 诊断结果.txt 2>&1
   ```

2. **尝试手动启动并保存日志**
   ```bash
   python web_assistant.py > 启动日志.txt 2>&1
   ```

3. **截图错误信息**

### 联系帮助

提供以下信息：
- 诊断结果.txt
- 启动日志.txt
- 错误截图
- 系统版本（Win + R → winver）

---

## ✅ 验证成功标志

启动成功后，你应该看到：

```
============================================================
     中国象棋 AI 助手 - Web 图形界面
============================================================

正在启动 Web 服务器...
运行模式: 普通模式
请在浏览器中打开: http://localhost:5000

按 Ctrl+C 停止服务
============================================================

 * Serving Flask app 'web_assistant' (lazy loading)
 * Environment: production
 * Debug mode: off
 * Running on http://0.0.0.0:5000/ (Press CTRL+C to quit)
```

**然后**：
1. 浏览器打开 `http://localhost:5000`
2. 应该能看到象棋界面
3. 点击"启动引擎"
4. 访问 `http://localhost:5000/mobile` 测试移动端

---

## 💡 预防措施

### 避免问题的最佳实践

1. **使用完整路径**
   - 不要用相对路径
   - 使用 `C:\ProgramData\Anaconda3\python.exe`

2. **确保依赖完整**
   ```bash
   pip install -r requirements.txt
   ```

3. **关闭冲突程序**
   - 关闭其他使用 5000 端口的程序
   - 停止旧的 Python 进程

4. **定期更新**
   ```bash
   pip install --upgrade Flask Werkzeug
   ```

---

**祝你成功启动！** 🚀

如果遇到其他问题，随时告诉我具体的错误信息！
