# 🔄 清除浏览器缓存 - 看到最新界面

## 问题

更新后还是看到旧界面？这是**浏览器缓存**导致的！

---

## ⚡ 快速解决方案

### 方案一：使用新网址（最快，推荐！）

**直接访问**：
```
http://192.168.100.102:5000/mobile?v=3
                                    ^^^^
                                    加这个参数
```

**原理**：浏览器认为这是新页面，不会使用缓存。

---

### 方案二：清除缓存（彻底）

#### Android Chrome

**简单方法**：
1. 地址栏长按网址
2. 选择 **"在新的隐私标签页中打开"**（无痕模式）
3. 无痕模式不会使用缓存

**完整方法**：
1. 点击右上角 **⋮**（三个点）
2. **设置** → **隐私设置和安全性**
3. **清除浏览数据**
4. 勾选 **"缓存的图片和文件"**
5. 点击 **"清除数据"**
6. 重新访问网址

#### iPhone Safari

1. **设置** → **Safari**
2. **清除历史记录与网站数据**
3. 确认清除
4. 重新访问网址

#### 电脑浏览器

**Chrome/Edge**：
- Windows: `Ctrl + Shift + Delete`
- Mac: `Cmd + Shift + Delete`
- 勾选"缓存的图片和文件"
- 清除

**Firefox**：
- Windows: `Ctrl + Shift + Delete`
- Mac: `Cmd + Shift + Delete`
- 选择"缓存"
- 清除

---

### 方案三：重启服务 + 强制刷新

1. **重启服务**
   ```bash
   # 停止（Ctrl + C）
   # 重新启动
   C:\ProgramData\Anaconda3\python.exe web_assistant.py
   ```

2. **手机访问新网址**
   ```
   http://192.168.100.102:5000/mobile?v=3
   ```

---

## ✅ 验证是否成功

成功后应该看到：

```
┌──────────────────────────┐
│ 象棋AI助手   🟢          │
├──────────────────────────┤
│                          │
│     炮二平五             │  ← 大号中文
│                          │
├──────────────────────────┤
│ 评估分数        +0.35    │
│ 搜索深度        18 层    │  ← 详细信息
│ 参考着法        h2e2     │
│ ┌──────────────────────┐ │
│ │ 主要变化：            │ │
│ │ h2e2 b9c7...         │ │  ← 主要变化
│ └──────────────────────┘ │
├──────────────────────────┤
│ [🔄 刷新][📊 候选][⚙️]  │  ← 三个按钮
└──────────────────────────┘
```

**关键特征**：
- ✅ 大号中文着法（36px）
- ✅ 有"评估分数"、"搜索深度"标签
- ✅ 有灰色背景的"主要变化"框
- ✅ 有"📊 候选"按钮

---

## 🎯 推荐步骤

### 最快方法（30秒）：

1. **重启服务**
   ```bash
   # 在命令提示符按 Ctrl + C
   # 然后运行
   C:\ProgramData\Anaconda3\python.exe web_assistant.py
   ```

2. **手机访问新网址**
   ```
   http://192.168.100.102:5000/mobile?v=3
   ```

3. **完成！**

---

## 💡 为什么会缓存？

浏览器为了加速访问，会保存页面的副本（缓存）。

**问题**：
- 服务端更新了
- 但浏览器还在用旧的缓存
- 所以看到的是旧界面

**解决**：
- 方法一：访问新网址（带参数 `?v=3`）
- 方法二：清除浏览器缓存
- 方法三：无痕模式（不使用缓存）

---

## 📞 如果还是不行

**拍照给我看**：
1. 当前看到的界面
2. 浏览器地址栏（确认网址）

**告诉我**：
1. 使用的浏览器（Chrome/Safari/其他）
2. 是否按 `?v=3` 访问
3. 是否重启了服务

---

## 🎊 立即试试

**在手机浏览器输入**：
```
http://192.168.100.102:5000/mobile?v=3
```

**应该立即看到新界面！**

如果还是不行，用**无痕模式**打开！
