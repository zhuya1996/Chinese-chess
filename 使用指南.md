# 中国象棋 AI 助手 - 快速使用指南

## ✅ 当前状态

- ✅ Pikafish 引擎已安装 (6.6 MB)
- ✅ 神经网络模型已安装 (48.4 MB)
- ✅ 引擎测试成功
- ✅ Web 服务可正常启动

## 🚀 快速启动

### 方法一：双击启动脚本（推荐）

```
双击运行: 启动服务_修复版.bat
```

### 方法二：命令行启动

```bash
C:\ProgramData\Anaconda3\python.exe web_assistant.py
```

### 方法三：原始脚本（需要修改 Python 路径）

编辑 `启动Web界面.bat`，将 `python` 改为 `C:\ProgramData\Anaconda3\python.exe`

## 📱 访问界面

启动后在浏览器打开：

- **本地访问**: http://localhost:5000
- **局域网访问**: http://192.168.100.101:5000

## 🎮 基本操作

### 1. 启动引擎
- 打开页面后，点击 **"启动引擎"** 按钮
- 等待状态显示 **"引擎就绪"**

### 2. 走棋
- **选子**: 点击要移动的棋子
- **落子**: 点击目标位置（或高亮的可走位置圆点）

### 3. 获取 AI 建议
- **快捷键 A** 或点击 **"获取建议"** 按钮
- 查看最佳着法、评估分数和主要变化

### 4. 一键应用建议
- 点击 **"建议并落子"** 按钮
- AI 的最佳着法会自动执行

### 5. 查看多个候选
- **快捷键 M** 或点击 **"多个候选"** 按钮
- 查看 1-5 个候选着法及评估

### 6. 其他操作
- **撤销**: 快捷键 Z 或点击 **"撤销"** 按钮
- **翻转棋盘**: 点击 **"翻转"** 按钮
- **重置棋局**: 点击 **"重置"** 按钮

## ⚙️ 高级功能

### 右侧面板 4 个 Tab

1. **着法记录** - 完整走棋历史
2. **候选&评估** - 分析结果、PV 主线、形势评估条
3. **识别助手** - Android 手机屏幕识别（需要 ADB）
4. **工具设置** - 搜索深度、AI 模式、FEN 工具、主题切换

### 搜索深度设置

- **固定深度**: 18（默认）- 适合快速分析
- **深度区间**: 18-22 - 更准确但较慢
- **调整建议**:
  - 开局/残局: 深度 15-18
  - 中局复杂局面: 深度 20-25
  - 快速对弈: 深度 12-15

### FEN 导入/导出

在 **"工具设置"** Tab 中：
- **导出当前局面**: 复制 FEN 字符串分享
- **导入局面**: 粘贴 FEN 字符串加载

## 🎨 棋盘主题

在 **"工具设置"** Tab 中切换：
- 🟤 古典木纹（默认）
- 🟡 米黄雅韵
- ⚫ 乌木深沉
- 🟢 碧玉青翠

## 🔧 故障排查

### 问题：引擎未响应
**解决**: 刷新页面，重新点击 **"启动引擎"**

### 问题：分析时间过长
**解决**: 点击 **"停止分析"** 或按 ESC 键，降低搜索深度

### 问题：Web 服务启动失败
**原因**: 端口 5000 被占用
**解决**: 
```bash
# 使用其他端口
C:\ProgramData\Anaconda3\python.exe web_assistant.py --port 8080
```

### 问题：编码错误（乱码）
**解决**: 
- 确保浏览器编码设置为 UTF-8
- Windows 命令行：运行 `chcp 65001`

## 📚 API 使用

如果你想通过程序调用：

### 启动引擎
```bash
curl -X POST http://localhost:5000/api/start
```

### 获取建议
```bash
curl -X POST http://localhost:5000/api/suggest \
  -H "Content-Type: application/json" \
  -d '{"depth": 18}'
```

### 添加着法
```bash
curl -X POST http://localhost:5000/api/move \
  -H "Content-Type: application/json" \
  -d '{"move": "b2e2"}'
```

### 获取状态
```bash
curl http://localhost:5000/api/status
```

## 📞 获取帮助

- **项目主页**: https://github.com/official-pikafish/Pikafish
- **问题反馈**: 项目 Issues 页面
- **文档**: README.md

## ⌨️ 快捷键汇总

| 快捷键 | 功能 |
|--------|------|
| A      | 获取建议 |
| M      | 多个候选 |
| Z      | 撤销 |
| ESC    | 停止分析 |

---

**享受你的象棋 AI 之旅！🎉**
