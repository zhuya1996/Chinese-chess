# 🌐 云端下载 APK 使用指南

## 无需 Android 开发环境，直接下载使用！

---

## 📥 快速下载（3 种方式）

### 方式一：GitHub Releases（推荐） ✅

1. **访问 Releases 页面**
   ```
   https://github.com/你的用户名/Chinese-chess/releases
   ```

2. **选择最新版本**
   - 点击最新的 `v1.0.0` 标签
   - 找到 **Assets** 部分

3. **下载 APK**
   - 点击 `app-debug.apk` 下载（普通用户推荐）
   - 或 `app-release-unsigned.apk`（高级用户）

4. **传输到手机**
   - 直接用手机浏览器下载
   - 或通过电脑下载后用微信/QQ 传到手机

### 方式二：GitHub Actions Artifacts

1. **访问 Actions 页面**
   ```
   https://github.com/你的用户名/Chinese-chess/actions
   ```

2. **选择最新构建**
   - 点击最上面的绿色 ✓ 构建
   - 滚动到页面底部

3. **下载 Artifacts**
   - `app-debug` - 测试版 APK
   - `app-release` - 发布版 APK

4. **解压并传输**
   - 下载的是 .zip 文件
   - 解压得到 .apk 文件
   - 传输到手机

### 方式三：自动构建（开发者）

每次 push 到 master 分支，GitHub 会自动构建 APK。

---

## 📱 安装到手机（1 分钟）

### 第一步：允许安装未知应用

**方法因手机品牌而异**：

#### 小米 / Redmi
1. 设置 → 隐私保护 → 特殊权限
2. 安装未知应用
3. 找到"文件管理"或"浏览器"，打开

#### 华为 / 荣耀
1. 设置 → 安全 → 更多安全设置
2. 安装外部来源应用
3. 选择"文件管理"或"浏览器"

#### OPPO / vivo
1. 设置 → 安全 → 安装未知应用
2. 选择用于安装的应用

#### 原生 Android
1. 设置 → 应用和通知 → 高级 → 特殊应用访问权限
2. 安装未知应用
3. 选择你的文件管理器

### 第二步：安装 APK

1. 用文件管理器找到下载的 `.apk` 文件
2. 点击文件
3. 点击"安装"
4. 等待安装完成

### 第三步：允许悬浮窗权限

安装后首次启动会请求权限，点击"允许"即可。

如果未弹出，手动设置：
1. 设置 → 应用管理 → 象棋AI助手
2. 权限管理 → 显示悬浮窗 → 允许

---

## 🚀 使用（2 分钟）

### 准备工作

1. **电脑端**：启动后台服务
   ```bash
   # 双击运行
   启动服务_修复版.bat
   
   # 记下显示的 IP，例如：
   # Running on http://192.168.1.100:5000/
   ```

2. **手机端**：连接同一 WiFi
   - 确保手机和电脑在同一个 WiFi 网络

### 配置 App

1. 打开"象棋AI助手"
2. 输入服务器地址：`http://192.168.1.100:5000`
   - 替换为你电脑显示的实际 IP
3. 点击"启动悬浮窗"
4. 打开 JJ 象棋
5. 悬浮窗会自动显示 AI 建议！

---

## 🎯 版本说明

### app-debug.apk（推荐）
- ✅ 体积较大（~10MB）
- ✅ 包含调试信息
- ✅ 安装简单
- ✅ 适合普通用户

### app-release-unsigned.apk
- ✅ 体积较小（~5MB）
- ❌ 未签名（部分手机可能无法安装）
- ✅ 性能优化
- ⚠️ 适合高级用户

**建议**：优先使用 `app-debug.apk`，安装成功率更高。

---

## 🔧 常见问题

### Q1: 找不到 Releases 页面？

**A**: 需要先创建第一个 Release：

```bash
# 在项目目录运行
git tag v1.0.0
git push origin v1.0.0
```

等待 5-10 分钟，GitHub Actions 会自动构建并发布。

### Q2: APK 下载很慢？

**A**: GitHub 在国内访问较慢，可以：
- 使用 GitHub 加速镜像（如 ghproxy.com）
- 使用 VPN
- 从 Actions Artifacts 下载（通常更快）

### Q3: 安装提示"解析软件包错误"？

**A**: 可能是下载不完整，解决方法：
1. 重新下载 APK
2. 检查文件大小（debug 版约 10MB）
3. 尝试用其他下载工具

### Q4: 手机浏览器无法下载 APK？

**A**: 部分浏览器（如 Chrome）会拦截 APK 下载：
- 使用手机自带浏览器
- 或使用微信/QQ 接收文件
- 或使用文件传输工具（如 Send Anywhere）

### Q5: 每次都要重新下载吗？

**A**: 不需要！只有以下情况需要更新：
- 新增功能
- 修复 Bug
- 性能优化

正常使用一次安装即可长期使用。

---

## 📊 下载方式对比

| 方式 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| GitHub Releases | 稳定、永久链接 | 需要手动创建 tag | ⭐⭐⭐⭐⭐ |
| GitHub Actions | 自动化、最新版本 | 需要登录 GitHub | ⭐⭐⭐⭐ |
| 本地构建 | 可自定义 | 需要开发环境 | ⭐⭐⭐ |

---

## 🌟 高级功能：自动发布新版本

### 发布新版本的步骤

```bash
# 1. 更新代码并提交
git add .
git commit -m "修复Bug / 添加新功能"

# 2. 创建版本标签
git tag v1.0.1  # 递增版本号

# 3. 推送到 GitHub
git push origin master
git push origin v1.0.1

# 4. 等待 5-10 分钟
# GitHub Actions 自动构建并发布到 Releases
```

### 版本号规范

- `v1.0.0` - 第一个正式版
- `v1.0.1` - Bug 修复
- `v1.1.0` - 新功能
- `v2.0.0` - 重大更新

---

## 💡 分享给朋友

### 分享链接

```
下载象棋AI助手：
https://github.com/你的用户名/Chinese-chess/releases/latest

或者直接发送 APK 文件
```

### 朋友需要准备什么？

1. **手机**：Android 7.0+
2. **电脑**：运行后台服务（Python + Pikafish）
3. **网络**：手机和电脑同一 WiFi

**提示**：朋友不需要安装 Android Studio 或任何开发工具，只需要你分享的 APK！

---

## 📚 相关文档

- [Android悬浮窗使用指南.md](./Android悬浮窗使用指南.md) - 完整功能说明
- [Android悬浮窗方案总结.md](./Android悬浮窗方案总结.md) - 技术实现
- [使用指南.md](./使用指南.md) - 整体项目说明

---

## ✅ 总结

### 传统方式（本地构建）
```
安装 Android Studio (4GB) 
→ 安装 JDK (500MB)
→ 配置环境变量
→ 下载 Gradle (200MB)
→ 等待构建 (10分钟)
→ 得到 APK
```
**总耗时**：1-2 小时
**难度**：⭐⭐⭐⭐

### 云端方式（推荐）
```
访问 GitHub Releases
→ 点击下载 APK (10MB)
→ 安装到手机
→ 完成
```
**总耗时**：2-5 分钟
**难度**：⭐

---

## 🎉 开始使用！

1. ✅ 访问 [Releases 页面](https://github.com/你的用户名/Chinese-chess/releases)
2. ✅ 下载 `app-debug.apk`
3. ✅ 安装到手机
4. ✅ 配置服务器地址
5. ✅ 享受 AI 辅助下棋！

**无需任何开发环境，简单快捷！** 🚀

---

有任何问题，随时在 GitHub Issues 提问！
