# 🚀 立即发布到 GitHub - 操作指南

## 当前状态
✅ 所有代码已准备就绪（已提交到本地 Git）
❌ 需要配置 GitHub 权限后推送

---

## 🔑 解决权限问题（2 种方法）

### 方法一：使用 Personal Access Token（推荐）

#### 第一步：生成 Token

1. **访问 GitHub Token 页面**
   ```
   https://github.com/settings/tokens
   ```

2. **点击 "Generate new token" → "Generate new token (classic)"**

3. **配置 Token**
   - Note: `Chinese-chess-push`
   - Expiration: `90 days` 或 `No expiration`
   - ✅ 勾选 `repo` （完整仓库权限）
   - ✅ 勾选 `workflow` （Actions 权限）

4. **点击 "Generate token"**
   - ⚠️ 复制生成的 token（只显示一次！）
   - 格式类似：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxx`

#### 第二步：使用 Token 推送

```bash
# 方法 A: 一次性推送（每次都要输入）
git push origin master
# Username: li-xiao-kun
# Password: [粘贴你的 token]

# 方法 B: 保存凭据（推荐）
git config credential.helper store
git push origin master
# 输入一次后就会保存
```

---

### 方法二：使用 SSH 密钥

#### 第一步：生成 SSH 密钥

```bash
# 1. 生成密钥
ssh-keygen -t ed25519 -C "li-xiao-kun@example.com"

# 提示时直接回车（使用默认路径）
# 可以设置密码或留空

# 2. 查看公钥
cat ~/.ssh/id_ed25519.pub

# 或在 Windows：
type %USERPROFILE%\.ssh\id_ed25519.pub
```

#### 第二步：添加到 GitHub

1. **访问 SSH 密钥页面**
   ```
   https://github.com/settings/keys
   ```

2. **点击 "New SSH key"**
   - Title: `我的电脑`
   - Key: 粘贴第一步复制的公钥
   - 点击 "Add SSH key"

#### 第三步：更改 Git 远程地址

```bash
# 从 HTTPS 改为 SSH
git remote set-url origin git@github.com:li-xiao-kun/Chinese-chess.git

# 验证
git remote -v

# 推送
git push origin master
```

---

## 📤 完整推送流程

### 解决权限后执行：

```bash
# 1. 推送代码
git push origin master

# 2. 创建版本标签
git tag -a v1.0.0 -m "首次发布：中国象棋AI助手"

# 3. 推送标签（触发自动构建）
git push origin v1.0.0

# 4. 等待构建（5-10 分钟）
```

---

## 🎯 或者使用一键脚本

配置好权限后，直接双击运行：

```
首次发布到GitHub.bat
```

脚本会自动：
1. ✅ 推送代码到 master
2. ✅ 创建标签 v1.0.0
3. ✅ 推送标签触发构建
4. ✅ 显示查看链接

---

## 🔗 构建完成后

### 查看构建进度
```
https://github.com/li-xiao-kun/Chinese-chess/actions
```

看到绿色 ✅ 表示成功

### 下载 APK
```
https://github.com/li-xiao-kun/Chinese-chess/releases
```

点击 `v1.0.0` → 下载 `app-debug.apk`

### 分享给用户
```
https://github.com/li-xiao-kun/Chinese-chess/releases/latest
```

用户点击链接即可下载！

---

## ❓ 常见问题

### Q: 忘记保存 Token 怎么办？
**A**: 重新生成一个新的，旧的会失效。

### Q: Token 过期了？
**A**: 重新生成一个，重新配置即可。

### Q: SSH 密钥不工作？
**A**: 检查：
```bash
# 测试连接
ssh -T git@github.com

# 应该看到：
# Hi li-xiao-kun! You've successfully authenticated...
```

### Q: 推送失败？
**A**: 检查：
1. 网络是否正常
2. GitHub 是否可访问
3. 用户名和 Token 是否正确
4. 是否有仓库写入权限

---

## 📋 快速检查清单

配置前：
- [ ] GitHub 账号可以登录
- [ ] 确认仓库属于你（li-xiao-kun/Chinese-chess）
- [ ] 有仓库管理权限

配置后：
- [ ] Token 已生成并保存
- [ ] ��� SSH 密钥已添加
- [ ] `git remote -v` 显示正确的地址
- [ ] 可以成功推送

推送后：
- [ ] GitHub 代码已更新
- [ ] Actions 开始构建
- [ ] 等待 5-10 分钟
- [ ] Releases 出现 APK

---

## 🎉 成功标志

全部完成后，你会看到：

1. **GitHub 仓库**
   - master 分支有最新代码
   - 有 `.github/workflows/build-android.yml` 文件
   - README.md 显示完整

2. **Actions 页面**
   - 有 "Build Android APK" workflow
   - 状态为绿色 ✅

3. **Releases 页面**
   - 有 v1.0.0 版本
   - 包含 2 个 APK 文件
   - 有版本说明

4. **用户可以**
   - 访问下载链接
   - 下载 APK 文件
   - 安装到手机使用

---

## 💡 下一步

配置好权限并推送后：

1. **测试 APK**
   - 下载到手机
   - 安装测试
   - 确认功能正常

2. **分享给朋友**
   - 发送 Releases 链接
   - 收集反馈
   - 优化改进

3. **持续更新**
   - 修改代码
   - 打新标签
   - 自动构建发布

---

**现在就开始配置吧！选择方法一（Token）或方法二（SSH），5 分钟搞定！** 🚀
