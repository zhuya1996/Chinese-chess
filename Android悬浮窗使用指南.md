# 象棋 AI 助手 - Android 悬浮窗快速使用指南

## 🎯 效果演示

```
┌─────────────────┐
│ JJ 象棋界面      │
│                 │
│  ┌───────────┐  │ ← 悬浮窗（可拖动）
│  │ 象棋AI助手 │  │
│  │ 炮8平5     │  │
│  │ +0.35      │  │
│  │ [刷新] [⚙️] │  │
│  └───────────┘  │
│                 │
│   棋盘区域       │
└─────────────────┘
```

---

## ⚡ 5分钟快速开始

### 第一步：启动电脑后台（1分钟）

```bash
# 双击运行
启动服务_修复版.bat
```

后台服务会显示：
```
正在启动 Web 服务器...
请在浏览器中打开: http://localhost:5000
Running on http://192.168.100.101:5000/  ← 记下这个IP
```

**记下你的电脑 IP 地址**（如 `192.168.100.101`）

### 第二步：安装 Android App（2分钟）

**方法一：使用 Android Studio（推荐）**

1. 打开 Android Studio
2. File → Open → 选择 `android_app` 文件夹
3. 等待 Gradle 同步完成
4. 连接手机（USB 调试模式）
5. 点击运行按钮 ▶️

**方法二：手动编译**

```bash
cd android_app
./gradlew assembleDebug
adb install app/build/outputs/apk/debug/app-debug.apk
```

### 第三步：配置和启动（2分钟）

1. **打开 App**
   - 在手机上打开"象棋AI助手"

2. **输入服务器地址**
   ```
   http://192.168.100.101:5000
   ```
   （替换为第一步记下的 IP）

3. **点击"启动悬浮窗"**
   - 首次使用会请求悬浮窗权限
   - 点击"允许"

4. **打开 JJ 象棋**
   - 进入对局界面
   - 悬浮窗会自动显示 AI 建议

---

## 📱 使用方法

### 基本操作

1. **拖动悬浮窗**
   - 长按悬浮窗任意位置即可拖动

2. **查看建议**
   - 悬浮窗会自动每 5 秒刷新
   - 显示最佳着法和评估值

3. **手动刷新**
   - 点击"🔄 刷新"按钮
   - 立即获取最新建议

4. **调整设置**
   - 点击"⚙️"按钮
   - 可调整搜索深度和自动刷新间隔

5. **停止服务**
   - 下拉通知栏
   - 点击"停止"按钮
   - 或在 App 主界面点击"停止服务"

### 高级功能

**调整搜索深度**：
- 点击"⚙️"
- 修改"搜索深度"（5-25）
- 深度越高越准确，但分析越慢

**自动刷新设置**：
- 关闭：不自动刷新
- 3秒：快速模式
- 5秒：平衡模式（推荐）
- 10秒：省电模式

---

## 🔧 完整设置步骤

### 电脑端设置

1. **检查 IP 地址**
   
   **Windows**：
   ```bash
   ipconfig
   # 查找 "IPv4 地址"
   ```

   **Mac/Linux**：
   ```bash
   ifconfig
   # 查找 "inet" 地址
   ```

2. **启动后台服务**
   ```bash
   python web_assistant.py
   # 或双击：启动服务_修复版.bat
   ```

3. **测试连接**
   - 浏览器访问 `http://你的IP:5000`
   - 应该能看到 Web 界面

4. **启动引擎**
   - 在 Web 界面点击"启动引擎"
   - 等待"引擎就绪"

### Android 端设置

1. **确保在同一 WiFi**
   - 手机和电脑必须连接同一个 WiFi
   - 不能用移动数据

2. **关闭防火墙（如有问题）**
   - Windows 防火墙可能阻止连接
   - 临时关闭测试：控制面板 → 防火墙 → 关闭

3. **授权悬浮窗权限**
   - 设置 → 应用 → 象棋AI助手
   - 权限 → 显示在其他应用上层 → 允许

---

## 🎨 界面说明

### 悬浮窗元素

```
┌──────────────────┐
│ 象棋AI助手   🟢   │  ← 标题 + 状态指示灯
│ 02:04:15         │  ← 当前时间
├──────────────────┤
│ 炮8平5           │  ← 最佳着法（大字）
│ 评估: +0.35      │  ← 评估值（颜色编码）
│ 深度: 18  02:04 │  ← 搜索深度 + 时间
├──────────────────┤
│ [🔄 刷新] [⚙️]   │  ← 操作按钮
└──────────────────┘
```

### 颜色说明

- **🟢 绿色圆点**：引擎在线，正常运行
- **🔴 红色圆点**：引擎离线或错误
- **黄色文字**：红方优势（评分 > 0.3）
- **红色文字**：黑方优势（评分 < -0.3）
- **白色文字**：局势均衡

---

## 🐛 常见问题

### Q1: 悬浮窗不显示？

**原因**：未授权悬浮窗权限

**解决**：
1. 设置 → 应用 → 象棋AI助手
2. 权限 → 显示在其他应用上层 → 允许
3. 重新启动 App

### Q2: 显示"网络错误"？

**原因**：无法连接到电脑后台

**检查清单**：
- [ ] 手机和电脑在同一 WiFi
- [ ] 电脑后台服务已启动
- [ ] IP 地址输入正确
- [ ] 电脑防火墙未阻止（临时关闭测试）

**测试方法**：
```bash
# 手机浏览器访问
http://你的电脑IP:5000

# 应该能看到 Web 界面
```

### Q3: AI 建议不更新？

**原因**：引擎未启动

**解决**：
1. 电脑浏览器打开 `http://localhost:5000`
2. 点击"启动引擎"按钮
3. 等待"引擎就绪"
4. 手机悬浮窗点击"刷新"

### Q4: 分析太慢？

**原因**：搜索深度过高

**解决**：
1. 点击悬浮窗"⚙️"按钮
2. 将深度调低到 10-12
3. 点击"刷新"测试

### Q5: 悬浮窗太大/太小？

**当前版本**：固定大小 320dp

**后续版本**：会添加尺寸调整功能

**临时方案**：
- 编辑 `floating_window.xml`
- 修改 `android:layout_width="320dp"`

### Q6: 如何关闭悬浮窗？

**方法一**（推荐）：
- 下拉通知栏
- 点击"停止"按钮

**方法二**：
- 打开 App 主界面
- 点击"停止服务"

**方法三**（强制）：
- 设置 → 应用 → 象棋AI助手
- 强行停止

---

## 💡 使用技巧

### 技巧1：快速切换位置

悬浮窗可以拖到屏幕任意位置：
- **顶部中间**：不挡棋盘，查看方便
- **右下角**：靠近操作区，快速参考
- **左上角**：不影响右手操作

### 技巧2：调整刷新频率

根据使用场景：
- **快棋模式**：3秒刷新
- **正常对局**：5秒刷新（推荐）
- **研究分析**：10秒或手动刷新

### 技巧3：搜索深度选择

- **开局**：深度 12-15（快速）
- **中局**：深度 18-20（平衡）
- **残局**：深度 22-25（准确）

### 技巧4：省电模式

长时间使用建议：
- 关闭自动刷新
- 手动点击刷新
- 不用时停止服务

### 技巧5：网络稳定性

- 使用 5GHz WiFi（速度更快）
- 距离路由器近一些
- 避免下载等占用带宽的操作

---

## 📊 性能指标

### 正常表现

- **连接延迟**：< 100ms
- **分析延迟**：1-3秒（深度15-18）
- **刷新间隔**：5秒（可调）
- **内存占用**：< 50MB
- **电量消耗**：轻度使用 ~5%/小时

### 网络要求

- **最低速度**：1 Mbps
- **推荐速度**：10 Mbps+
- **延迟**：< 50ms

---

## 🔐 隐私与安全

### 数据传输

- ✅ 仅在本地局域网内通信
- ✅ 不连接互联网
- ✅ 不上传任何数据
- ✅ 不收集个人信息

### 权限说明

- **悬浮窗权限**：显示悬浮窗（必需）
- **网络权限**：连接电脑后台（必需）

### 使用建议

- ✅ 仅在练习模式使用
- ✅ 学习研究 AI 着法
- ❌ 不建议在竞技赛中使用
- ❌ 可能违反游戏规则

---

## 🛠️ 开发者选项

### 修改默认设置

编辑 `MainActivity.java`:
```java
private static final String DEFAULT_SERVER = "http://192.168.1.100:5000";
```

### 修改悬浮窗样式

编辑 `templates/mobile_float.html`:
```css
.compact-card {
    min-width: 280px;  /* 修改宽度 */
    max-width: 320dp;
}
```

### 调试模式

在 `FloatingWindowService.java` 添加：
```java
webView.setWebContentsDebuggingEnabled(true);
```

然后在电脑 Chrome 访问 `chrome://inspect`

---

## 📞 获取帮助

### 问题诊断

1. **检查电脑服务**
   ```bash
   curl http://localhost:5000/api/status
   ```

2. **检查网络连接**
   ```bash
   # 手机浏览器访问
   http://电脑IP:5000/mobile
   ```

3. **查看日志**
   - Android Studio → Logcat
   - 过滤：`com.xiangqi.aiassistant`

### 反馈渠道

- **项目主页**：提交 Issue
- **邮件反馈**：附带截图和日志

---

## 🎉 开始使用

准备好了吗？

1. ✅ 电脑启动后台服务
2. ✅ 安装 Android App
3. ✅ 输入服务器地址
4. ✅ 启动悬浮窗
5. ✅ 打开 JJ 象棋

**立即体验 AI 辅助下棋！** 🎊

---

## 📝 更新日志

### v1.0 (当前版本)
- ✅ 基础悬浮窗功能
- ✅ WebView 显示
- ✅ 自动/手动刷新
- ✅ 可调整搜索深度
- ✅ 可拖动位置

### 计划功能
- [ ] 多候选着法
- [ ] 形势评估图
- [ ] 历史记录
- [ ] 语音播报
- [ ] 主题切换
- [ ] 尺寸调整
- [ ] 透明度设置
