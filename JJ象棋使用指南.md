# JJ 象棋 AI 助手使用指南

## 📱 功能说明

通过 **Android 手机屏幕识别**，实时识别 JJ 象棋 App 的棋局，并获得 AI 分析建议。

### 工作原理
1. 手机通过 USB 连接电脑（或无线 ADB）
2. AI 助手实时抓取手机屏幕截图
3. 识别 JJ 象棋棋盘上的棋子位置
4. 自动同步局面到 Web 界面
5. AI 引擎分析并给出最佳着法建议
6. **可选**：自动执行 AI 建议的着法（点击手机屏幕）

---

## 🔧 准备工作

### 1. 安装 ADB 工具

**方法一：完整 Android SDK（推荐开发者）**
- 下载 Android Studio: https://developer.android.com/studio
- 安装后 ADB 位于：`C:\Users\你的用户名\AppData\Local\Android\Sdk\platform-tools\`

**方法二：独立 ADB 工具（推荐普通用户）**
- 下载地址: https://developer.android.com/studio/releases/platform-tools
- 解压到任意目录（如 `C:\adb\`）
- 将目录添加到系统 PATH 环境变量

**验证安装**：
```bash
# 打开命令提示符（CMD），输入：
adb version

# 应该显示类似：
# Android Debug Bridge version 1.0.41
```

### 2. 手机设置（Android）

#### 2.1 启用开发者选项
1. 打开 **设置** → **关于手机**
2. 连续点击 **版本号** 7次
3. 提示 "您已处于开发者模式"

#### 2.2 启用 USB 调试
1. 返回 **设置** → **系统** → **开发者选项**
2. 开启 **USB 调试**
3. （可选）开启 **USB 调试（安全设置）** - 允许模拟点击

#### 2.3 连接电脑
1. 用 USB 数据线连接手机和电脑
2. 手机会弹出 **允许 USB 调试？** 对话框
3. 勾选 **始终允许来自这台计算机**
4. 点击 **确定**

**验证连接**：
```bash
adb devices

# 应该显示：
# List of devices attached
# XXXXXX    device        ← 你的手机设备号
```

如果显示 `unauthorized`，检查手机是否允许了 USB 调试。

### 3. 安装 JJ 象棋 App

- 从应用商店下载安装 **JJ象棋**
- 或从官方渠道下载 APK：http://www.jj.cn/
- 登录账号，进入棋盘界面

---

## 🎮 使用流程

### 第一步：启动 Web 服务

```bash
双击：启动服务_修复版.bat
```

浏览器访问：http://localhost:5000

### 第二步：启动引擎

在 Web 界面点击 **"启动引擎"** 按钮

### 第三步：准备手机

1. **连接手机**
   ```bash
   # 在命令行验证
   adb devices
   ```

2. **打开 JJ 象棋 App**
   - 进入对局界面（不管是人人对战还是人机对战）
   - 确保棋盘完整显示在屏幕上
   - 建议使用**标准棋盘皮肤**（默认皮肤识别率最高）

### 第四步：启动识别助手

在 Web 界面右侧切换到 **"识别助手"** Tab，点击 **"启动 JJ 识别"** 按钮。

**首次使用需要校准**：

#### 4.1 四角校准（重要！）
识别助手会显示手机屏幕画面，你需要：

1. **点击棋盘左上角**（红方车的位置）
2. **点击棋盘右上角**（红方车的位置）
3. **点击棋盘左下角**（黑方车的位置）
4. **点击棋盘右下角**（黑方车的位置）

⚠️ **注意**：点击的是棋盘格子的**交叉点**，不是棋子中心！

#### 4.2 确认视角
选择你的视角：
- **红方在下**（默认）- 红方棋子在屏幕底部
- **黑方在下** - 黑方棋子在屏幕底部

点击 **"确认校准"** 完成设置。

#### 4.3 生成识别模板
校准完成后，系统会自动：
- 识别所有棋子类型（车、马、炮、相、士、将、兵）
- 生成棋子模板（保存到 `recognition_profiles/` 目录）
- 下次使用直接加载，无需重新校准

### 第五步：实时识别与分析

校准完成后，识别助手会：

1. **实时抓取手机屏幕**（每秒 1-2 帧）
2. **识别当前棋局**
   - 显示识别置信度（建议 > 78%）
   - 显示当前 FEN 局面
3. **自动同步到 Web 界面**
   - 棋盘自动更新
   - 显示识别的局面
4. **AI 分析**
   - 手动点击 **"获取建议"** 或按 `A` 键
   - 查看最佳着法和评估

### 第六步：应用 AI 建议

**方式一：手动走棋**
- 在 JJ 象棋 App 中手动走 AI 建议的着法

**方式二：自动执行（实验性功能）**
- 在识别助手界面点击 **"自动执行着法"**
- 系统会模拟点击手机屏幕，自动走棋
- ⚠️ **使用风险提示**：
  - 可能违反 JJ 象棋的服务条款
  - 可能导致账号被封禁
  - 仅供学习研究使用

---

## ⚙️ 识别设置

### 置信度阈值
- **默认值**：78%
- **调整建议**：
  - 识别准确但同步慢 → 降低到 70-75%
  - 识别不准确 → 提高到 80-85%

### 稳定帧数
- **默认值**：2 帧
- **说明**：连续 N 帧识别结果一致才同步
- **调整建议**：
  - 快速响应 → 设为 1
  - 更稳定识别 → 设为 3-5

### 棋盘皮肤
- 支持 JJ 象棋默认皮肤（识别率最高）
- 其他皮肤可能需要重新校准

---

## 🐛 常见问题

### 问题 1：adb devices 无设备

**可能原因**：
- USB 线接触不良
- 未开启 USB 调试
- ADB 驱动未安装

**解决方案**：
```bash
# 重启 ADB 服务
adb kill-server
adb start-server
adb devices
```

### 问题 2：识别置信度低（< 50%）

**可能原因**：
- 棋盘未完整显示
- 使用了非标准皮肤
- 光线太暗或反光严重

**解决方案**：
- 重新进行四角校准
- 切换到默认棋盘皮肤
- 调整手机亮度和拍摄角度
- 在 "识别助手" Tab 点击 **"重新校准"**

### 问题 3：识别结果不同步

**检查**：
- 置信度是否 > 78%
- 稳定帧数是否达标
- Web 界面是否显示 "等待识别启动"

**解决方案**：
- 降低置信度阈值到 70%
- 降低稳定帧数到 1
- 点击 **"停止识别"** 再 **"启动识别"**

### 问题 4：无法自动执行着法

**可能原因**：
- 未开启 **USB 调试（安全设置）**
- 系统不支持模拟输入
- 屏幕坐标计算错误

**解决方案**：
1. 手机 **开发者选项** → 开启 **USB 调试（安全设置）**
2. 检查 ADB 权限：
   ```bash
   adb shell settings put secure enabled_input_methods
   ```
3. 重新进行四角校准

### 问题 5：识别卡顿或延迟高

**可能原因**：
- USB 连接不稳定
- 手机性能不足
- 电脑 CPU 占用高

**解决方案**：
- 使用质量好的 USB 数据线
- 关闭手机其他 App
- 降低识别频率（修改代码中的 `time.sleep` 值）

---

## 📊 识别助手界面说明

### 状态指示

| 指示项 | 说明 |
|--------|------|
| 设备连接 | ✓ 已连接 / ✗ 未连接 |
| 识别运行 | ✓ 运行中 / ✗ 已停止 |
| 校准状态 | ✓ 已校准 / ✗ 未校准 |
| 置信度 | 当前识别的可信程度（0-100%） |
| 稳定帧 | 连续识别一致的帧数 |
| 同步次数 | 成功同步的局面数量 |

### 实时预览

- 显示手机屏幕截图
- 叠加显示识别的棋盘框
- 显示棋子识别结果

### 操作按钮

| 按钮 | 功能 |
|------|------|
| 启动 JJ 识别 | 开始识别流程 |
| 停止识别 | 停止识别并释放资源 |
| 重新校准 | 重新进行四角校准 |
| 自动执行着法 | 模拟点击手机执行 AI 建议 |

---

## 🔐 安全与合规提示

### ⚠️ 重要提醒

1. **学习用途**
   - 本功能仅供学习象棋、分析棋局使用
   - 不建议在竞技排位赛中使用

2. **账号风险**
   - 使用自动执行功能可能违反游戏规则
   - 可能导致账号被封禁或限制
   - **风险自负，谨慎使用**

3. **合法合规**
   - 尊重游戏运营商的服务条款
   - 不建议在金钱相关的对局中使用
   - 建议仅在练习模式或人机对战中使用

### 建议的使用场景

✅ **推荐**：
- 复盘分析（导入棋谱）
- 研究开局定式
- 学习残局技巧
- 人机对战练习

❌ **不推荐**：
- 竞技排位赛
- 真人对战
- 锦标赛或比赛
- 涉及金钱的对局

---

## 🛠️ 高级配置

### 无线 ADB（不用 USB 线）

如果手机和电脑在同一 WiFi：

```bash
# 1. 先用 USB 连接，启用无线调试
adb tcpip 5555

# 2. 拔掉 USB 线

# 3. 连接手机 WiFi IP（手机设置 → 关于手机 → 状态信息中查看）
adb connect 192.168.1.xxx:5555

# 4. 验证连接
adb devices
```

### 自定义识别参数

编辑 [recognition_companion.py](recognition_companion.py) 修改：

```python
MIN_CONFIDENCE = 0.78          # 最小置信度（0.7-0.9）
STABLE_FRAME_TARGET = 2        # 稳定帧数（1-5）
TEMPLATE_SIZE = 52             # 模板尺寸（48-64）
```

### 识别配置文件

位置：`recognition_profiles/jj_xiangqi_android_default.json`

包含：
- 棋盘四角坐标
- 棋子模板路径
- 视角方向
- 校准时间戳

**备份建议**：校准成功后备份整个 `recognition_profiles/` 目录

---

## 📹 完整操作视频流程

### 文字版快速流程

1. 启动 Web 服务（`启动服务_修复版.bat`）
2. 浏览器打开 http://localhost:5000
3. 点击"启动引擎"
4. USB 连接手机，开启 USB 调试
5. 打开 JJ 象棋 App，进入对局
6. Web 界面切换到"识别助手" Tab
7. 点击"启动 JJ 识别"
8. 依次点击棋盘四角完成校准
9. 等待识别稳定（置信度 > 78%）
10. 点击"获取建议"或按 `A` 键
11. 查看 AI 分析结果
12. 在 JJ 象棋中走棋

---

## 💡 实战技巧

### 提高识别准确率

1. **光线充足** - 避免反光和阴影
2. **标准皮肤** - 使用 JJ 象棋默认棋盘
3. **保持静止** - 识别时不要移动手机
4. **清晰截图** - 确保棋盘完整、清晰
5. **定期校准** - 更换设备或 App 更新后重新校准

### 快速上手建议

1. **第一次**：在人机对战中测试，熟悉流程
2. **调整参数**：根据识别效果调整置信度
3. **建立模板库**：在标准初始局面完成首次校准
4. **验证准确性**：对比 FEN 字符串与实际棋局

---

## 📞 获取帮助

- **识别问题**：检查 [recognition_companion.py](recognition_companion.py) 日志
- **ADB 问题**：运行 `adb logcat | grep -i "error"`
- **Web 界面问题**：浏览器按 F12 查看控制台

---

**🎉 祝你使用愉快，棋艺精进！**

**⚠️ 再次提醒：请遵守游戏规则，理性使用辅助工具！**
