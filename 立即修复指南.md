# ⚡ 立即修复 - 重启服务加载新功能

## 问题

1. **悬浮窗版显示 UCI** (`h9g7`) 而不是中文（马8进7）
2. **完整版 404 错误** - 找不到页面

## 原因

**服务没有重启！** 

代码已经更新了，但是旧的服务还在运行，没有加载新代码。

---

## ⚡ 立即解决（3步）

### 第一步：停止旧服务

**在运行服务的命令提示符窗口**：

1. 按 **Ctrl + C** 停止服务
2. 如果窗口已经关闭，**双击运行**：`强制重启服务.bat`

---

### 第二步：重新启动

**双击运行**：`强制重启服务.bat`

**应该看到**：
```
[1/3] 强制停止所有 Python 进程...
      [OK] 已停止

[2/3] 清理端口占用...
      [OK] 端口已清理

[3/3] 启动新服务...
      新功能已加载！

* Running on http://192.168.100.102:5000/
```

---

### 第三步：清除浏览器缓存

**手机浏览器**：

#### 方法一：访问新网址（最简单）

**悬浮窗版（中文着法）**：
```
http://192.168.100.102:5000/mobile?v=5
                                    ^^^^
                                    加这个参数
```

**完整版（带棋盘）**：
```
http://192.168.100.102:5000/mobile/full
```

#### 方法二：无痕模式

1. 地址栏长按网址
2. 选择 **"在新的隐私标签页中打开"**

---

## ✅ 验证成功

### 悬浮窗版 (`/mobile?v=5`)

**应该看到**：
```
┌──────────────────────┐
│ 象棋AI助手   🟢      │
├──────────────────────┤
│                      │
│     马８进７         │  ← 中文！不是 h9g7
│                      │
│ 评估分数    -0.18    │
│ 搜索深度    15 层    │
│ 参考着法    h9g7     │  ← UCI 在这里（小字）
│ ┌──────────────────┐ │
│ │ 主要变化：        │ │
│ │ h9g7 b2e2...     │ │
│ └──────────────────┘ │
├──────────────────────┤
│ [🔄 刷新][📊 候选]   │
└──────────────────────┘
```

**关键**：大号显示的是 **"马８进７"**，不是 `h9g7`！

---

### 完整版 (`/mobile/full`)

**应该看到**：
```
┌──────────────────────┐
│ 🟢 象棋AI助手        │
├──────────────────────┤
│ ┌──────────────────┐ │
│ │ [车][马][象]...  │ │  ← 棋盘！
│ │                  │ │
│ │ [炮]      [炮]   │ │
│ │                  │ │
│ └──────────────────┘ │
├──────────────────────┤
│ AI 分析建议          │
│   炮二平五           │  ← 中文着法
├──────────────────────┤
│ [🚀 启动][💡 建议]  │
└──────────────────────┘
```

**关键**：能看到棋盘，不是 404 错误！

---

## 🎯 完整操作流程

### 1. 强制重启服务

```
双击运行：强制重启服务.bat
```

**等待看到**：
```
* Running on http://192.168.100.102:5000/
```

---

### 2. 测试悬浮窗版（中文着法）

**手机浏览器访问**：
```
http://192.168.100.102:5000/mobile?v=5
```

**操作**：
1. 点击 "🔄 刷新"
2. **应该显示中文**（马８进７）
3. **不是** `h9g7`

---

### 3. 测试完整版（��盘）

**手机浏览器访问**：
```
http://192.168.100.102:5000/mobile/full
```

**操作**：
1. **应该看到棋盘**
2. 点击 "🚀 启动引擎"
3. 点击 "💡 获取建议"
4. **应该显示中文着法**

---

## 📋 故障排查

### 问题 1：还是显示 `h9g7`

**原因**：浏览器缓存

**解决**：
```
http://192.168.100.102:5000/mobile?v=6
                                    ^^^^
                                    改成 v=6 或 v=7
```

或用**无痕模式**打开

---

### 问题 2：完整版还是 404

**原因**：服务没重启

**解决**：
1. 关闭命令提示符窗口
2. 双击 `强制重启服务.bat`
3. 重新访问 `/mobile/full`

---

### 问题 3：启动失败

**症状**：
```
Address already in use
```

**解决**：
1. 打开任务管理器（Ctrl+Shift+Esc）
2. 找到所有 `python.exe` 进程
3. 右键 → 结束任务
4. 重新运行 `强制重启服务.bat`

---

## 🎊 成功标志

### ✅ 悬浮窗版

- 大号显示：**"马８进７"**（中文）
- 小字显示：`UCI: h9g7`
- 有 "主要变化" 灰色框
- 有 "📊 候选" 按钮

### ✅ 完整版

- 能看到**完整棋盘**
- 能看到所有**棋子**
- 有 "🚀 启动引擎" 按钮
- 有 "💡 获取建议" 按钮
- **不是** 404 错误

---

## 📞 如果还是不行

**告诉我**：

1. 是否已经运行 `强制重启服务.bat`？
2. 命令提示符显示什么？
3. 浏览器地址栏是什么？
4. 看到什么错误？

**截图给我看**！

---

## 🎯 现在立即操作

**第一步**：
```
关闭命令提示符窗口（如果还开着）
```

**第二步**：
```
双击运行：强制重启服务.bat
```

**第三步**：
```
手机访问：
http://192.168.100.102:5000/mobile?v=5
http://192.168.100.102:5000/mobile/full
```

---

**🎉 立即试试！重启后一定能看到中文和棋盘！**
